-- 4.1 Colorscheme
-- To switch color schemes on the fly, use <leader>Pc (:Telescope colorscheme)

lvim.colorscheme = "everforest"

-- lvim.colorscheme = "catppuccin"
-- lvim.colorscheme = "onedarker"

-- Transparent Windows (DON'T make window transparent)
-- lvim.transparent_window = true

--4.2 Statusline
-- LunarVim uses lualine as a default statusline.
-- lvim.builtin.lualine.options.theme = "auto"        -- let it change dynamically.